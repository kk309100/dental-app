-- ============================================================================
-- 見積書に「題名」を追加できるようにする
-- 2026-09-24
--
-- 背景: 見積書を作成・一覧表示する際に、内容が分かるタイトルを
--       付けられるようにしたい。quotes テーブルに空きカラムが無いため、
--       新規カラムを追加する。
-- Supabase Studio の SQL Editor で1回実行してください。
-- ============================================================================

ALTER TABLE IF EXISTS quotes ADD COLUMN IF NOT EXISTS title text;
