-- ============================================================================
-- 見積書の明細に「定価」を保存できるようにする
-- 2026-09-12
--
-- 背景: 見積書作成画面では定価を入力できるが、quote_items テーブルに
--       定価を保存するカラムが無く、印刷画面などで表示できなかった。
-- Supabase Studio の SQL Editor で1回実行してください。
-- ============================================================================

ALTER TABLE IF EXISTS quote_items ADD COLUMN IF NOT EXISTS list_price integer;
